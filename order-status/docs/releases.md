# Releases

`mpt-extension-contrib-order-status` is released independently with a
`order-status-<version>` tag.

Release this package only when its own API or declared dependencies change.

Follow the repository-wide [release workflow](../../docs/releases.md).
