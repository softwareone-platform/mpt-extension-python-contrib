# Testing

Tests for `{{ distribution }}` live under [`../tests/`](../tests). Replace the
generated example test with coverage for the real module behavior.

Use package-scoped test commands with `pkg={{ module }}`. See the
repository-wide [testing strategy](../../docs/testing.md).
