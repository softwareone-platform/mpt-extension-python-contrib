# Usage

`{{ distribution }}` is a contrib package scaffold.

## Install

```bash
pip install {{ distribution }}
```

## Example

```python
from mpt_extension_contrib.{{ python_name }} import normalize_example

normalize_example("  Example Value  ")  # -> "example value"
```

Replace this walkthrough with real install, configuration, and usage steps for
the module's public API before the first release.
