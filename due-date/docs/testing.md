# Testing

Tests for `mpt-extension-contrib-due-date` live under [`../tests/`](../tests).
Cover due-date calculation behavior and edge cases in this module.

Use package-scoped test commands with `pkg=due-date`. See the repository-wide
[testing strategy](../../docs/testing.md).
