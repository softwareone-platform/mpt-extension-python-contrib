# Testing

Tests for `mpt-extension-contrib-due-date` live under [`../tests/`](../tests). Replace the
generated example test with coverage for the real module behavior.

Use package-scoped test commands with `pkg=due-date`. See the
repository-wide [testing strategy](../../docs/testing.md).
