# mpt-extension-contrib-shared

Internal shared utilities for packages in this repository.

The import path lives under the contrib namespace:

```python
from mpt_extension_contrib.shared import normalize_token
```

This distribution is internal. Extension repositories should not depend on it
directly.
