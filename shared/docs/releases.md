# Releases

`mpt-extension-contrib-shared` is released independently with a
`shared-<version>` tag.

Release `shared` before any consumer that adopts its new version. A breaking
change does not require updating every consumer in the same pull request.

Follow the repository-wide [release workflow](../../docs/releases.md).
