# Testing

Tests for `mpt-extension-contrib-shared` live under [`../tests/`](../tests).
Cover helper behavior directly and add consumer tests in a public module when
an integration contract changes.

Use package-scoped test commands with `pkg=shared`. See the repository-wide
[testing strategy](../../docs/testing.md).
