# Contributing

Keep helpers in `mpt_extension_contrib.shared` generic and reusable across
contrib modules. Treat this package as internal API for this repository.

Use package-scoped validation with `pkg=shared`. Follow the repository-wide
[contributing workflow](../../docs/contributing.md) for dependency changes,
validation commands, and pre-commit expectations.
